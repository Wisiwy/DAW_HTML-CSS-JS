/* class Operacion {

    constructor(num1, num2) {
        this.operando1 = num1;
        this.operando2 = num2;
        this._resultado = 0; // Inicializamos resultado
    }

    suma() {
        this._resultado = this.operando1 + this.operando2;
    }

    resta() {
        this._resultado = this.operando1 - this.operando2;
    }

    multiplicacion() {
        this._resultado = this.operando1 * this.operando2;
    }

    division() {
        this._resultado = this.operando1 / this.operando2;
    }

    eligeOperacion(signo) {
        switch (signo) {
            case '+':
                this.suma();
                break;
            case '-':
                this.resta();
                break;
            case '*':
                this.multiplicacion();
                break;
            case '/':
                this.division();
                break;
            default:
                alert("Operación equivocada");
                break;
        }
    }

    get resultado() {
        return this._resultado;
    }
}
 */